import React from "react"

class ConnButton extends React.Component {
    render() {
        return (
            <button className="conButton">
                Connexion
            </button>
        )
    }
}

export default ConnButton